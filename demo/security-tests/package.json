{
  "name": "@auth-lab/security-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone adversarial auditor for the auth demo. Probes both /baseline and /hardened, emits docs/findings.json.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "audit": "node run.mjs",
    "audit:baseline": "node run.mjs --stack=baseline",
    "audit:hardened": "node run.mjs --stack=hardened"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
